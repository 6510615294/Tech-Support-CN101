services:
  frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://go-api:8080/api
    ports:
      - "3000:3000"
    depends_on:
      - api
    environment:
      - NODE_ENV=production